diff --git a/xxhash.h b/xxhash.h
index 78fc2e8..6dc8258 100644
--- a/xxhash.h
+++ b/xxhash.h
@@ -3847,7 +3847,8 @@ XXH_PUBLIC_API XXH64_hash_t XXH64_hashFromCanonical(XXH_NOESCAPE const XXH64_can
 #  if defined(__ARM_NEON__) || defined(__ARM_NEON) \
    || (defined(_M_ARM) && _M_ARM >= 7) \
    || defined(_M_ARM64) || defined(_M_ARM64EC) \
-   || (defined(__wasm_simd128__) && XXH_HAS_INCLUDE(<arm_neon.h>)) /* WASM SIMD128 via SIMDe */
+   || (defined(__wasm_simd128__) && XXH_HAS_INCLUDE(<arm_neon.h>) && \
+       (!defined(XXH_VECTOR) || XXH_VECTOR != XXH_SCALAR)) /* WASM SIMD128 via SIMDe */
 #    define inline __inline__  /* circumvent a clang bug */
 #    include <arm_neon.h>
 #    undef inline

