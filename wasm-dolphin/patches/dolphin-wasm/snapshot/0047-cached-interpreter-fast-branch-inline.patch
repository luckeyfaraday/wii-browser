diff --git a/Source/Core/Core/PowerPC/CachedInterpreter/CachedInterpreter.cpp b/Source/Core/Core/PowerPC/CachedInterpreter/CachedInterpreter.cpp
--- a/Source/Core/Core/PowerPC/CachedInterpreter/CachedInterpreter.cpp
+++ b/Source/Core/Core/PowerPC/CachedInterpreter/CachedInterpreter.cpp
@@ -51,6 +51,20 @@
 #include "Core/PowerPC/PowerPC.h"
 #include "Core/System.h"
 
+#ifdef __EMSCRIPTEN__
+#ifndef DOLPHIN_WEB_INLINE_FAST_BRANCH
+#define DOLPHIN_WEB_INLINE_FAST_BRANCH 1
+#endif
+
+#if DOLPHIN_WEB_INLINE_FAST_BRANCH
+#define DOLPHIN_WEB_FAST_BRANCH_INLINE __attribute__((always_inline))
+#else
+#define DOLPHIN_WEB_FAST_BRANCH_INLINE
+#endif
+#else
+#define DOLPHIN_WEB_FAST_BRANCH_INLINE
+#endif
+
 #ifdef __EMSCRIPTEN__
 #include <emscripten/emscripten.h>
 #include <mbedtls/sha256.h>
@@ -9073,8 +9087,8 @@ static bool TryFastRamMultiwordLoadStore(Core::System& system, PowerPC::PowerPCS
   return true;
 }
 
-s32 CachedInterpreter::FastBranch(PowerPC::PowerPCState& ppc_state,
-                                  const FastInstructionOperands& operands)
+DOLPHIN_WEB_FAST_BRANCH_INLINE s32 CachedInterpreter::FastBranch(
+    PowerPC::PowerPCState& ppc_state, const FastInstructionOperands& operands)
 {
   const auto& [power_pc, current_pc, inst] = operands;
   (void)power_pc;
